public class run {
    
    for(int i=0;i<myStr.length-1;i++)
            {
                if()
                {
                    myChu=myStr[i+1];
                    myStr[i+1]=myStr[i];
                    myStr[i]=myChu;
                }
                
               // System.out.println(myStr[i]);
            }
            
}
